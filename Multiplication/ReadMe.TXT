

   For the multiplication machine,the requirements are as follows:

   1. The machine computes the multiplication of two numbers without using multiplier. 

   2. The machine takes two 16-bit numbers from user.

   3. In order to see multiplication result, the user must push start button.